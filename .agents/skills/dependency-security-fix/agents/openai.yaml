interface:
  display_name: "Dependency Security Fix"
  short_description: "Apply PatchPilot's recommended package updates"
  default_prompt: "Use $dependency-security-fix to apply the supplied PatchPilot fix targets, regenerate package-lock.json, and verify the requested versions without repeating dependency analysis."
